services:
  backend:
    build:
      context: ./apps/backend
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build:
      context: ./apps/frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped
